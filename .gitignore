# IDE cache
.cache/

# Build artifacts and external dependencies
resources/
ndi-sdk/
build/
obj-x86_64-linux-gnu/

# Debian packaging (keep tools, ignore artifacts)
debian/*.deb
debian/*.changes
debian/*.buildinfo
debian/files
debian/*.substvars

# Debian packages are built in parent dir
*.deb
*.dsc
*.changes
