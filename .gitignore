# LLM tooling (optional - can be tracked if desired)
.clineignore
.clinerules

# IDE cache
.cache/

# Build artifacts and external dependencies
resources/
ndi-sdk/
build/

# Debian packages are built in parent dir
*.deb
*.dsc
*.changes
